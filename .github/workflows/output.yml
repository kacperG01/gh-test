name: Output information
on: workflow_dispatch
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output GitHub context
      #To pozwala na umieszczanie metadanych
        run: echo "${{ toJSON(github) }}"
